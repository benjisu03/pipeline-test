platform: linux
image_resource:
  type: docker-image
  source:
    repository: alpine/git
    tag: 1.0.7
inputs:
  - name: repo
run:
  path: ../prepare-project.sh
params:
  MAJOR_VERSION:
  GITHUB_ORGANIZATION:
  GITHUB_REPOSITORY: